packages:
  - 'apps/*'
  - 'packages/*'

# pnpm v10 blocks dependency install scripts unless explicitly allowed.
# sharp needs its install script for prebuilt libvips binaries.
allowBuilds:
  sharp: true
